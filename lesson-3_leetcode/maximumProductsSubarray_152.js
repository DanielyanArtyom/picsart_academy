const test = [2, 3, -2, 4];

const maxProduct = function (nums) {
  let currentMax = 1;
  let currentMin = 1;
  let result = nums[0]

  if(nums.length === 1) {
    return nums[0]
  }

  for (let i = 1; i < nums.length; ++i) {
    
}

//   result = max(result,)
  return result
};
